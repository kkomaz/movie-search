<nav class="navbar navbar-default navbar-fixed-top">
  <div class="navbar-header" style="padding-top: 10px">
    <%= link_to "Home Page", root_path, class: "navbar-brand" %>
  </div>
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav pull-right">
    <% if current_user %>
      <li><%= link_to "Welcome Back #{current_user.name}", user_movies_path(current_user) %></li>
      <li><%= link_to "Log out", logout_path %></li>
      <li>
        <div class="form-group" style="margin-top: 15px">
          <%= form_tag(movies_path) do %>
            <%= label_tag('movie','Movie Search', class: 'search') %>
            <%= text_field_tag('movie')%>
            <%= submit_tag 'Search'%>
          <% end %>
        </div>
      </li>
    <% else %>
      <li><%= link_to "Sign in", login_path %></li>
      <li><%= link_to "Sign up", sign_up_path %></li>
    <% end %>
    </ul>
  </div>
</nav>